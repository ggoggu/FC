/task "GAS 기반의 [스킬/어트리뷰트 이름] 시스템을 구현해줘.

1. 구현 명세:
   - 클래스: [예: UCombatAttributeSet 또는 UGA_MeleeAttack]
   - 속성/기능: [예: Rage(분노) 어트리뷰트, 공격 시 분노 10 소모, 부족 시 발동 차단]
   - 태그: `Ability.Skill.[이름]`, `State.Combat.[상태]` 사용

2. 네트워크 및 권한:
   - 어트리뷰트는 GAMEPLAYATTRIBUTE_REPNOTIFY 매크로를 준수하고 OnRep 콜백 구현.
   - 스킬 발동은 로컬 예측(Local Predicted), 데미지 연산 및 상태 태그 부여는 서버 권한으로 실행.

3. 하네스 루프 지시:
   - 코드 작성 후 `python Scripts/verify_gas.py`와 `python Scripts/build_harness.py`를 실행해줘.
   - 검증 실패나 컴파일 에러 발생 시 외부 도움 없이 자율적으로 코드를 수정하여 패스할 때까지 루프를 돌아줘."





/task "Fast Array Serialization을 적용한 [인벤토리/무기 슬롯] 시스템을 구현해줘.

1. 구현 명세:
   - 구조체: `FWeaponEntry : FFastArraySerializerItem`, `FWeaponInventoryList : FFastArraySerializer`
   - 컴포넌트: `UWeaponManagerComponent`
   - 기능: 무기 추가/제거 시 `PostReplicatedAdd`, `PreReplicatedRemove` 델타 동기화.

2. 네트워크 및 최적화:
   - 단순 TArray 리플리케이션 금지 (대역폭 최적화 준수).
   - 무기 목록 동기화는 소유 클라이언트(`COND_OwnerOnly`)에게만 전달되도록 설정.

3. 하네스 루프 지시:
   - `python Scripts/audit_bandwidth.py` 및 `python Scripts/build_harness.py`를 실행해줘.
   - 대역폭 경고 및 컴파일 에러가 없을 때까지 자체 수정 루프를 수행해줘."



/task "[무기 유형/이름]의 데미지 및 밸런스 수치를 조정해줘.

1. 목표 밸런스 기준:
   - 대상: 체력 100, 경갑(방어력 20), 중갑(방어력 50)
   - 목표 TTK: 경갑 기준 [예: 2.2초], 중갑 기준 [예: 3.8초] 내외

2. 하네스 시뮬레이션 지시:
   - `python Scripts/simulate_ttk_balance.py --benchmark`를 실행하여 현재 세팅의 TTK와 DPS 곡선을 분석해줘.
   - 목표 TTK에 수렴하도록 BaseDamage, AttackSpeed, CriticalMultiplier 수치를 튜닝하고 결과를 마크다운 표로 보고해줘."


/task "[구현된 기능 이름, 예: 대시 스킬 / 히트스캔 사격]의 네트워크 안정성을 검증해줘.

1. 검증 절차:
   - `python Scripts/simulate_net_pie.py`를 실행하여 150ms 지연 및 5% 패킷 손실 조건에서 2인 세션(Server + Client) 헤드리스 테스트를 수행해줘.
   - 로그에서 위치 롤백(Correction) 횟수, RPC 드랍, 어설션 실패 여부를 파싱해줘.
   - 문제 발견 시 C++ 소스(`Source/`)의 예측 로직 또는 보정 수치를 자체 수정한 후 재테스트해줘."
